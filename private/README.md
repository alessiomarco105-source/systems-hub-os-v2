# Private Local Data

This directory is ignored except for this file.

Do not place secrets here unless the future runtime encrypts and controls access. Prefer an external secret manager. Private business or personal data should normally live in a separate restricted repository or encrypted store and be supplied to models only as approved extracts.
